{%- comment -%}
  ============================================================
  DEWS — YOUR OWN MANUAL CSS
  ============================================================
  Whatever you paste below loads LAST on every page, after
  style.css and after every section stylesheet. So it always wins.

  HOW TO EDIT
    Shopify Admin
      -> Online Store -> Themes -> ... -> Edit code
      -> snippets / dews-custom-css.liquid
      -> paste between the <style> tags below -> Save

  WHY THIS FILE
    Section images and titles live in templates/*.json and
    config/settings_data.json. Uploading a whole theme zip
    overwrites those files, which is why your section values
    reset. CSS pasted here is code, not settings — it stays put.

  IMPORTANT
    Send the contents of this file to your developer whenever you
    add something by hand, so it can be folded into the theme zip
    as well. Otherwise a future zip upload will replace it.
{%- endcomment -%}

<style>
  /* ---- paste your manual CSS below this line ---- */




  /* ---- paste your manual CSS above this line ---- */
</style>
